# Go build output
/dist/
*.test

# Node / Vite
web/node_modules/
web/dist/*
!web/dist/index.html

# Editor / OS
.DS_Store
*.swp

# Local Claude Code settings
.claude/settings.local.json

# Runtime working directory (if created in-tree)
mongosync-ui-data/

# Test environment replica-set keyfile (generated, secret)
testenv/.keyfile

# mongosync runtime metrics output
metrics/
